# metameme-bot

TG bot for creating memes written in Python.

## To run

### Create and edit .env:

```sh
cp .env.example .env
vim .env
```

### Create venv:

```sh
make venv
```

### Install dependencies:

```sh
make install
```

### Run:

```sh
make run
```